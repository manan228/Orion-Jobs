services:
  - name: selenium-app
    type: web
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "./start.sh"
    envVars:
      - key: SENDGRID_API_KEY
        sync: false
